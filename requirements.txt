openai>=1.40.0
feedparser>=6.0.10
httpx>=0.27.0
PyYAML>=6.0
ruamel.yaml>=0.18
python-dateutil>=2.9.0
fpdf2>=2.7.4

# Optional: PDF carousel generation works out of the box (fpdf2 above).
# Optional: podcast transcription is opt-in. Pick ONE tier and install it
# separately, since each pulls heavy deps you may not want by default:
#   pip install "podcast-transcriber[openai]"    # OpenAI Whisper API
#   pip install "podcast-transcriber[gemini]"    # Google Gemini
#   pip install "podcast-transcriber[local]"     # local whisper.cpp
